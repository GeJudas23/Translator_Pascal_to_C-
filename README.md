# Транслятор Pascal → C++

**Курсовой проект по дисциплине "Теория языков программирования и компиляторы"**

Транслятор преобразует программы, написанные на подмножестве языка PascalABC.NET, в эквивалентный код на языке C++.

---

## 📋 Содержание

- [Возможности](#возможности)
- [Архитектура](#архитектура)
- [Установка](#установка)
- [Использование](#использование)
- [Поддерживаемые конструкции](#поддерживаемые-конструкции)
- [Примеры](#примеры)
- [Структура проекта](#структура-проекта)
- [Технические детали](#технические-детали)

---

# Возможности

- **Лексический анализ** — распознавание токенов Pascal с обработкой комментариев
- **Синтаксический анализ** — построение AST методом рекурсивного спуска
- **Генерация кода C++** — преобразование AST в читаемый код C++
- **Поддержка подпрограмм** — процедуры и функции с параметрами
- **Массивы** — одномерные и двумерные с автоматической корректировкой индексов
- **Детальная диагностика ошибок** — точное указание позиции ошибки

---

# Архитектура

Транслятор состоит из четырех основных модулей:

```
┌─────────────────┐
│  Исходный код   │
│     Pascal      │
└────────┬────────┘
         │
         ▼
┌─────────────────┐      ┌──────────────┐
│   Лексический   │──────│   Токены     │
│   анализатор    │      └──────────────┘
└────────┬────────┘
         │
         ▼
┌─────────────────┐      ┌──────────────┐
│  Синтаксический │──────│  AST дерево  │
│   анализатор    │      └──────────────┘
└────────┬────────┘
         │
         ▼
┌─────────────────┐      ┌──────────────┐
│   Генератор     │──────│  Код C++     │
│      кода       │      └──────────────┘
└─────────────────┘
```

### Модули:

1. **`lexer.py`** — Лексический анализатор
   - Разбивает исходный код на токены
   - Обрабатывает комментарии: `{ }`, `(* *)`, `//`
   - Распознает ключевые слова, идентификаторы, литералы
   - Обработка ошибок с указанием строки и столбца

2. **`ast_nodes.py`** — Узлы AST
   - Классы для представления структуры программы
   - Выражения, операторы, объявления

3. **`parser.py`** — Синтаксический анализатор
   - Строит AST методом рекурсивного спуска
   - Проверяет синтаксическую корректность программы
   - Детальная диагностика синтаксических ошибок

4. **`codegen.py`** — Генератор кода C++
   - Обход AST и генерация C++ кода
   - Преобразование типов и операторов
   - Корректировка индексов массивов
   - Форматирование кода с отступами

5. **`translator.py`** — Главное приложение
   - CLI интерфейс
   - Координация работы всех модулей

---

# Установка

### Требования:
- Python 3.7 или выше
- Нет внешних зависимостей (используется только стандартная библиотека)

### Скачивание:

```bash
# Создайте директорию проекта
mkdir pascal_to_cpp
cd pascal_to_cpp

# Разместите файлы:
# - lexer.py
# - ast_nodes.py
# - parser.py
# - codegen.py
# - translator.py
```

---

# спользование

### Базовое использование:

```bash
# Транслировать program.pas в program.cpp
python translator.py program.pas

# Указать имя выходного файла
python translator.py program.pas -o output.cpp

# Подробный вывод процесса трансляции
python translator.py program.pas -v

# Все опции вместе
python translator.py program.pas -o result.cpp -v
```

### Пример вывода:

```
============================================================
✓ Трансляция успешно завершена!
  Входной файл:  factorial.pas
  Выходной файл: factorial.cpp
============================================================
```

### С подробным выводом (`-v`):

```
============================================================
ЭТАП 1: Лексический анализ
============================================================
Найдено токенов: 45

Первые 20 токенов:
  Token(PROGRAM, 'program', 1:1)
  Token(IDENTIFIER, 'Factorial', 1:9)
  Token(SEMICOLON, ';', 1:18)
  ...

============================================================
ЭТАП 2: Синтаксический анализ
============================================================
Программа: Factorial
Переменных: 2
Подпрограмм: 1

============================================================
ЭТАП 3: Генерация кода C++
============================================================
Сгенерировано строк кода: 28

============================================================
✓ Трансляция успешно завершена!
============================================================
```

---

# Поддерживаемые конструкции

### Типы данных:
- `integer` → `int`
- `real` → `double`
- `boolean` → `bool`
- `char` → `char`
- `string` → `string`
- Массивы: `array[1..10] of integer`

### Операторы:

#### Арифметические:
- `+`, `-`, `*`, `/`
- `div` → `/` (целочисленное деление)
- `mod` → `%` (остаток)

#### Логические:
- `and` → `&&`
- `or` → `||`
- `not` → `!`
- `xor` → `^`

#### Сравнения:
- `=` → `==`
- `<>` → `!=`
- `<`, `<=`, `>`, `>=`

#### Присваивание:
- `:=` → `=`

### Управляющие конструкции:

```pascal
// Условный оператор
if condition then
    statement
else
    statement;

// Цикл while
while condition do
    statement;

// Цикл repeat
repeat
    statements
until condition;

// Цикл for
for i := 1 to 10 do
    statement;

for i := 10 downto 1 do
    statement;

// Оператор case
case expr of
    1: statement1;
    2, 3: statement2;
    else statement3
end;
```

### Подпрограммы:

```pascal
// Процедура
procedure PrintHello;
begin
    writeln('Hello');
end;

// Функция
function Add(a, b: integer): integer;
begin
    Add := a + b;
end;

// Параметры по ссылке
procedure Swap(var a, b: integer);
var temp: integer;
begin
    temp := a;
    a := b;
    b := temp;
end;
```

### Встроенные функции:

| Pascal | C++ |
|--------|-----|
| `write()` | `cout <<` |
| `writeln()` | `cout << ... << endl` |
| `read()` | `cin >>` |
| `readln()` | `cin >>` |
| `abs()` | `abs()` |
| `sqr(x)` | `x * x` |
| `sqrt()` | `sqrt()` |
| `sin()`, `cos()` | `sin()`, `cos()` |
| `ln()` | `log()` |
| `exp()` | `exp()` |
| `length(s)` | `s.length()` |

---

# Примеры

### Пример 1: Факториал

**Входной файл (factorial.pas):**

```pascal
program Factorial;

var
    n, result: integer;

function fact(x: integer): integer;
begin
    if x <= 1 then
        fact := 1
    else
        fact := x * fact(x - 1)
end;

begin
    write('Enter a number: ');
    readln(n);
    result := fact(n);
    writeln('Factorial = ', result)
end.
```

**Выходной файл (factorial.cpp):**

```cpp
#include <iostream>
#include <string>
#include <cmath>

using namespace std;

int fact(int x);

int main() {
    int n;
    int result;

    cout << "Enter a number: ";
    cin >> n;
    result = fact(n);
    cout << "Factorial = " << result << endl;
    return 0;
}

int fact(int x) {
    int fact_result;

    if ((x <= 1)) {
        fact_result = 1;
    } else {
        fact_result = (x * fact((x - 1)));
    }
    return fact_result;
}
```

### Пример 2: Работа с массивами

**Pascal:**

```pascal
program ArraySum;

var
    arr: array[1..5] of integer;
    i, sum: integer;

begin
    sum := 0;
    for i := 1 to 5 do
    begin
        arr[i] := i * 10;
        sum := sum + arr[i];
    end;
    writeln('Sum = ', sum)
end.
```

**C++:**

```cpp
#include <iostream>
#include <string>
#include <cmath>

using namespace std;

int main() {
    int arr[5];
    int i;
    int sum;

    sum = 0;
    for (int i = 1; i <= 5; i++) {
        arr[(i - 1)] = (i * 10);
        sum = (sum + arr[(i - 1)]);
    }
    cout << "Sum = " << sum << endl;
    return 0;
}
```

Обратите внимание на автоматическую корректировку индексов массива с `[1..5]` на `[0..4]`.

---

# Структура проекта

```
pascal_to_cpp/
│
├── lexer.py          # Лексический анализатор
├── ast_nodes.py      # Определение узлов AST
├── parser.py         # Синтаксический анализатор
├── codegen.py        # Генератор кода C++
├── translator.py     # Главное приложение
│
├── examples/         # Примеры программ
│   ├── factorial.pas
│   ├── arrays.pas
│   └── procedures.pas
│
└── README.md         # Этот файл
```

---

# Технические детали

### Лексический анализ

**Конечный автомат** распознает:
- Ключевые слова (program, var, begin, end, if, while, ...)
- Идентификаторы (начинаются с буквы или `_`)
- Числовые литералы (целые и вещественные, включая экспоненциальную форму)
- Строковые литералы (`'...'` и `"..."`)
- Операторы (`:=`, `<>`, `<=`, `>=`, `..`)
- Разделители (`;`, `,`, `.`, `()`, `[]`)
- Комментарии (`{ }`, `(* *)`, `//`)

### Синтаксический анализ

**Метод рекурсивного спуска:**
- Каждому нетерминалу грамматики соответствует функция
- Грамматика преобразована для устранения левой рекурсии
- Построение AST дерева в процессе разбора

**Приоритет операций:**
1. `not`, унарный `+`, `-`
2. `*`, `/`, `div`, `mod`, `and`
3. `+`, `-`, `or`, `xor`
4. `=`, `<>`, `<`, `<=`, `>`, `>=`

### Генерация кода

**Преобразования:**
- Программа → функция `main()`
- Процедуры → функции с типом `void`
- Функции → функции с явным `return`
- Массивы с произвольной индексацией → корректировка к 0-based индексам
- Присваивание имени функции → присваивание `function_result`

**Стандартная библиотека:**
- `#include <iostream>` — для ввода/вывода
- `#include <string>` — для работы со строками
- `#include <cmath>` — для математических функций

---

# Обработка ошибок

Транслятор предоставляет детальную диагностику:

### Лексические ошибки:

```
✗ Лексическая ошибка: Lexer error at 5:12: Незавершенный строковый литерал
```

### Синтаксические ошибки:

```
✗ Синтаксическая ошибка: Parser error at 8:5: Ожидается SEMICOLON, получено END
```

---

## 🎓 Образовательная ценность

Проект демонстрирует:
- Принципы построения компиляторов
- Фазы трансляции программы
- Лексический и синтаксический анализ
- Работу с абстрактными синтаксическими деревьями
- Генерацию целевого кода
- Обработку ошибок на всех этапах

---

## 📝 Ограничения

Текущая версия не поддерживает:
- Записи (records) и указатели
- Динамическое выделение памяти
- Множества (sets)
- Перечисления (enums)
- Модули (units)
- Файловый ввод-вывод
- Исключения

---

## 🚀 Возможные улучшения

- Семантический анализ с проверкой типов
- Таблица символов с областями видимости
- Оптимизация генерируемого кода
- Поддержка дополнительных конструкций Pascal
- Режим интерактивной отладки
- Юнит-тесты для всех компонентов

---

## 👥 Авторы

**Курсовой проект выполнили:**
- Антонов Г.А. (группа Б9122-09.03.04)
- Березницкий Д.А. (группа Б9122-09.03.04)

**Руководитель:**
- Дилла Д.С., ассистент ДПИиИИ

**Учебное заведение:**
Дальневосточный федеральный университет (ДВФУ)  
Институт математики и компьютерных технологий  
Департамент программной инженерии и искусственного интеллекта

---

## 📄 Лицензия

Образовательный проект. Создан в учебных целях.

---

## 🙏 Благодарности

Особая благодарность:
- Никлаусу Вирту за создание языка Pascal
- Бьёрну Страуструпу за создание языка C++
- Преподавателям ДВФУ за руководство проектом

---

**Владивосток, 2025**